// 基础表达式和语句测试
int main() {
    // 变量声明与初始化
    int a = 10;
    int b = -5;
    
    // 算术表达式
    int c = a + b * 2;
    
    // 关系表达式
    if (c > 0 && a < 20) {
        c = c % 3;
    }
    
    // 逻辑表达式
    int d = !(a == b) || (b != 0);
    
    return c + d;
}